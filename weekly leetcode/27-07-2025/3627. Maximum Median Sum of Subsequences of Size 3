class Solution {
public:
    long long maximumMedianSum(vector<int>& nums) {
        sort(nums.begin(), nums.end());
        int n = nums.size(), m = n / 3, j = n - 2;
        long long ans = 0;
        for (int i = 0; i < m; ++i, j -= 2)
            ans += nums[j];
        return ans;
    }
};
